$alwaysValid = [letter digit _ % +]

pattern:

EMAIL {
  EMAIL_LOCALPART { (
    $alwaysValid 
    (
      [letter digit _ % + - .] 0..
      $alwaysValid
    ) 0..1
  ) 1..64 }
  "@"
  EMAIL_DOMAIN { @domain_unwrapped }
}
